import { DaysCounterPipe } from './days-counter.pipe';

describe('DaysCounterPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysCounterPipe();
    expect(pipe).toBeTruthy();
  });
});
